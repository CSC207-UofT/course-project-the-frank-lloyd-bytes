/**
 * This test class checks that the polymorphic command class has no type conflicts with the various User classes that it interacts with.
 * primarily it should test a search mechanism to find informtion and check against system data. IE passwords, duplicates
 * group access, discount access,
 */

public class UserCommandsTest {
}
